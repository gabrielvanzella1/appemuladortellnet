<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- AppBar -->
    <LinearLayout
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/primary"
        android:gravity="center_vertical"
        android:padding="12dp"
        android:elevation="8dp">

        <!-- Botão voltar às sessões sem desconectar -->
        <ImageButton
            android:id="@+id/btn_back_sessions"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@android:drawable/ic_menu_revert"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Voltar às sessões"
            android:tint="@color/white"
            android:layout_marginEnd="4dp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/statusText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/app_name"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Badge de sessão: mostra "⇄ Sessão 1" quando há 2 sessões ativas -->
        <TextView
            android:id="@+id/tv_session_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⇄"
            android:textColor="@color/secondary"
            android:textSize="20sp"
            android:layout_marginEnd="4dp"
            android:padding="4dp"
            android:visibility="gone" />

        <ImageButton
            android:id="@+id/keyboardToggle"
            android:layout_width="40dp"
            android:layout_height="34dp"
            android:src="@drawable/ic_keyboard"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Mostrar/ocultar teclado"
            android:layout_marginEnd="6dp"
            android:visibility="gone" />

        <Button
            android:id="@+id/disconnectButton"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:text="Desconectar"
            android:textSize="11sp"
            android:textColor="@color/white"
            android:background="@color/primary_dark"
            android:enabled="false"
            android:paddingStart="10dp"
            android:paddingEnd="10dp" />
    </LinearLayout>

    <!-- Terminal Output -->
    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#000000"
        android:fillViewport="true">

        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none">

            <com.logisticapp.emuladortelnet.terminal.TerminalView
                android:id="@+id/terminalOutput"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="10sp"
                android:fontFamily="monospace"
                android:padding="6dp"
                android:background="#000000"
                android:textColor="#00FF00"
                android:includeFontPadding="false"
                android:lineSpacingExtra="0dp" />
        </HorizontalScrollView>
    </ScrollView>

    <!-- Barras de ferramentas (4 linhas geradas dinamicamente) -->
    <LinearLayout
        android:id="@+id/controlKeysBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#000000"
        android:padding="2dp"
        android:visibility="gone" />

</LinearLayout>
