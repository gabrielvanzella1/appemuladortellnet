<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/primary"
            app:title="Configuração do leitor de..."
            app:titleTextColor="@android:color/white"
            app:navigationIcon="@drawable/ic_arrow_back" />
    </com.google.android.material.appbar.AppBarLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="32dp">

            <!-- Tipo de dispositivo -->
            <LinearLayout android:id="@+id/row_device_type" style="@style/TnRow">
                <TextView style="@style/TnLabel"
                    android:text="Tipo de dispositivo leitor de código de barras" />
                <TextView android:id="@+id/val_device_type" style="@style/TnValue" />
                <ImageView style="@style/ConfigChevron" />
            </LinearLayout>
            <View style="@style/TnDivider" />

            <!-- Espaçador visual -->
            <View android:layout_width="match_parent" android:layout_height="8dp" />

            <!-- Ação após verificação -->
            <LinearLayout android:id="@+id/row_action" style="@style/TnRow">
                <TextView style="@style/TnLabel" android:text="Ação após verificação" />
                <TextView android:id="@+id/val_action" style="@style/TnValue" />
                <ImageView style="@style/ConfigChevron" />
            </LinearLayout>
            <View style="@style/TnDivider" />

            <!-- Remover caracteres no início -->
            <LinearLayout android:id="@+id/row_remove_start" style="@style/TnRow">
                <TextView style="@style/TnLabel" android:text="Remover caracteres no início" />
                <TextView android:id="@+id/val_remove_start" style="@style/TnValue" />
                <ImageView style="@style/ConfigChevron" />
            </LinearLayout>
            <View style="@style/TnDivider" />

            <!-- Remover caracteres no final -->
            <LinearLayout android:id="@+id/row_remove_end" style="@style/TnRow">
                <TextView style="@style/TnLabel" android:text="Remover caracteres no final" />
                <TextView android:id="@+id/val_remove_end" style="@style/TnValue" />
                <ImageView style="@style/ConfigChevron" />
            </LinearLayout>
            <View style="@style/TnDivider" />

            <!-- Adicione texto antes -->
            <LinearLayout style="@style/TnRow">
                <TextView style="@style/TnLabel" android:text="Adicione texto antes" />
                <EditText
                    android:id="@+id/et_text_before"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:inputType="text"
                    android:gravity="end"
                    android:textSize="14sp"
                    android:background="@null"
                    android:padding="4dp" />
            </LinearLayout>
            <View style="@style/TnDivider" />

            <!-- Adicione texto depois -->
            <LinearLayout style="@style/TnRow">
                <TextView style="@style/TnLabel" android:text="Adicione texto depois" />
                <EditText
                    android:id="@+id/et_text_after"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:inputType="text"
                    android:gravity="end"
                    android:textSize="14sp"
                    android:background="@null"
                    android:padding="4dp" />
            </LinearLayout>
            <View style="@style/TnDivider" />

            <!-- Usar mapeamento de teclado -->
            <LinearLayout android:id="@+id/row_keyboard_mapping" style="@style/TnRow">
                <TextView style="@style/TnLabel" android:text="Usar mapeamento de teclado" />
                <CheckBox android:id="@+id/ck_keyboard_mapping" style="@style/TnCheck" />
            </LinearLayout>
            <View style="@style/TnDivider" />

            <!-- Mostrar na linha de status -->
            <LinearLayout android:id="@+id/row_show_status" style="@style/TnRow">
                <TextView style="@style/TnLabel" android:text="Mostrar na linha de status" />
                <CheckBox android:id="@+id/ck_show_status" style="@style/TnCheck" />
            </LinearLayout>
            <View style="@style/TnDivider" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
